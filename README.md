# SOC---Stage-2---Network-Traffic-Analysis
In Wireshark I saw HTTP on TCP with GET request in plain text. HTTPS showed TLS handshake with Client Hello and Server Hello then traffic was encrypted. DNS resolved domain to IP. Following TCP stream showed readable HTTP and encrypted HTTPS. TLS stream showed hello only.
